import styled from "styled-components";

export const SpanArrow = styled.span`
  i {
    color: white;
    position: absolute;
    z-index: 1;
    margin-left:5px;
    margin-top: 5px;

    :hover {
      color: gold;
      cursor: pointer;
    }
  }
`;
